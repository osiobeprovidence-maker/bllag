# ---- Convex (Backend) ----
# Local: auto-generated by `npx convex dev` in .env.local
# Production: set via `CONVEX_DEPLOY_KEY` in Vercel env vars.
# The build command in vercel.json handles VITE_CONVEX_URL automatically.
# VITE_CONVEX_URL="https://your-project.convex.cloud"

# ---- Paystack (Payments) ----
VITE_PAYSTACK_PUBLIC_KEY="pk_test_xxxxxxxxxxxxx"
# PA YSTACK_SECRET_KEY - set in Convex env (not exposed to client)
# npx convex env set PAYSTACK_SECRET_KEY "sk_test_xxxxxxxxxxxxx"

# ---- Mux (Video) ----
VITE_MUX_TOKEN_ID="your-mux-token-id"
VITE_MUX_TOKEN_SECRET="your-mux-token-secret"

# ---- Resend (Email) ----
# Used by Convex action to send magic link and transactional emails
# npx convex env set RESEND_API_KEY "re_xxxxxxxxxxxxx"
